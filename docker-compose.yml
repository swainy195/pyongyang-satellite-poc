services:
  api:
    build:
      context: ./apps/api
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./apps/api:/app
    command: uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
